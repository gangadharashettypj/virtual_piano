dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }// ory is going to shut down soon
    }
}
rootProject.name = "PianoKeys"
include ':app'
include ':virtual_piano'
